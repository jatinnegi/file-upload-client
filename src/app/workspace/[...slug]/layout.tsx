export const metadata = {
  title: "Stack Drive | Workspace",
  description: "Stack drive user workspace",
};

export default function WorkspaceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
